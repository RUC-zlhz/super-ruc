# S71 真实部署事实回填校正

- 日期：`2026-06-07`
- 关联主计划：`S71.5`
- 当前状态：`[x]` 已完成

## 背景

用户补充要求两份新文档必须“以真实代码实现为准，尤其以远端服务器 `http://10.10.0.13/` 的真实部署为准”。`2026-06-06` 的首版 `S71` 已完成模板正规化、脚本化出件和页面 QC，但文档口径仍混用了部分历史互测说明与旧共享账号事实，因此需要基于线上真实响应再做一次校正。

## 目标

- 在不改动业务代码的前提下，按 `10.10.0.13` 当前真实可观测行为重写《用户使用说明书》和《软件测试报告》中的访问、登录、验证与风险口径。
- 明确区分：
  - 当前线上已经在线核对的事实
  - 仅由当前代码实现和历史专项验证支撑、但本轮未拿到有效生产账号复测的能力
- 重新生成正式 DOCX，并重新执行 Word -> PDF -> PNG 版式 QC。

## 非范围

- 不修改后端、Web、Miniapp 业务实现。
- 不对生产环境执行任何写操作、强行重置账号或尝试猜测新密码。
- 不补跑一轮新的全量后端 `pytest`；本轮仅对线上事实与既有测试结论做重新收口。

## 线上核对事实

- `GET http://10.10.0.13/`
  - `200`
  - 页面标题为 `信息学院管理后台`
  - 当前静态资源指向 `/assets/index-CTe-2Tbm.js` 与 `/assets/index-5iSv60ht.css`
- `GET http://10.10.0.13/healthz`
  - `200`
  - 返回 `{"code":0,"message":"ok","data":{"status":"ok"}}`
- `GET http://10.10.0.13/api/v1/knowledge/search?page=1&page_size=20`
  - `200`
  - 返回 `total=16`
  - 当前公开知识结果中可看到休学、复学、校历、学院公告等官方条目，以及 `4` 个模板下载类条目
- `GET http://10.10.0.13/api/v1/knowledge/categories`
  - `200`
  - 返回 `9` 个分类：盖章、党员发展、团员管理、奖助学金、学业管理、证明与盖章、请假与销假、日常事务、其他
- `GET http://10.10.0.13/api/v1/knowledge/templates?page=1&size=10`
  - `401`
  - 返回“缺少 Authorization Bearer token”
- `GET http://10.10.0.13/api/v1/workflow/public/templates`
  - `401`
  - 返回“缺少 Authorization Bearer token”
- `POST http://10.10.0.13/api/v1/auth/login`
  - 使用历史共享口令 `admin / admin123`
  - 返回 `401`
  - 返回“工号或密码错误”

## 本轮改动

- 更新 `scripts/docs/build_project_user_manual.py`
  - 将访问方式、登录说明、FAQ 和知识库/模板管理表述改为以真实线上行为和当前前端路由为准
  - 去掉把 `admin / admin123` 当作当前有效线上共享账号的写法
  - 明确小程序应以当前 `mp-weixin` 产物或部署维护方提供的测试包为准
- 更新 `scripts/docs/build_project_test_report.py`
  - 将测试口径改为“`2026-05-26` 全量回归 + `2026-06-06` PR #6 专项复核 + `2026-06-07` 生产在线抽检”
  - 新增线上根页、健康检查、知识搜索、知识分类、受保护接口和失效共享口令的事实表述
  - 明确区分“线上已验证”与“仅代码/历史专项验证通过，但本轮未实登复核”
- 更新 `scripts/docs/docx_common.py`
  - 文档日期改为默认读取真实运行日期，不再写死旧日期

## 出件与 QC

- 重新生成：
  - `output/doc/用户使用说明书-信息学院学生综合服务与党团管理平台-v1.0.docx`
  - `output/doc/软件测试报告-信息学院学生综合服务与党团管理平台-v1.0.docx`
- 使用本机 Word 导出 PDF，再用 `pdftoppm` 渲染页面 PNG 做人工 QC
- 本轮最终页数：
  - 用户使用说明书：`12` 页
  - 软件测试报告：`8` 页

## 风险与后续

- 本轮没有拿到新的有效生产账号，因此管理端登录后功能、党团流程、事务审批和通知操作并未在 `10.10.0.13` 上重做一轮实登复测。
- 后续任何正式验收都应由部署维护方发放当前有效的管理员、教师和学生账号，不再继续引用旧互测文档中的 `admin / admin123` 口径。
- 若后续需要“以当前 HEAD 为准的最新完整测试报告”，仍需单独补跑一轮新的后端全量回归、Web build、Miniapp build 和真实账号联调。
