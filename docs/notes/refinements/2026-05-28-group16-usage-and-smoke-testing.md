# 第 16 组使用说明解析与接口烟测

- 日期：`2026-05-28`
- 状态：`[x]`
- 关联主计划条目：`S29.9`
- 测试方：`第12组`
- 被测方：`第16组`
- 说明文档来源：`C:\Users\24391\Downloads\a83c2e68371e43ccbecfaf4f48bfbb6e.zip`

## 已完成

- [x] 解包并读取第 16 组使用说明文档，确认包含 `UI -5.28.zip` 与 `学院学生综合服务平台使用说明文档.docx`。
- [x] 抽取运行环境、测试账号、服务地址、接口清单、功能范围和已知限制，形成 `docs/testing/group16-usage-summary.md`。
- [x] 解包 UI 源码并核对小程序请求配置、页面入口和主要接口调用方式。
- [x] 对第 16 组 Web、后端、数据库、登录和核心学生接口进行最小接口级烟测。
- [x] 整理 bug 草稿，形成 `docs/testing/group16-bug-report-drafts.md`；其中 `BUG-G16-001` 经页面级 Network 响应复核后否决，当前仅 `BUG-G16-002` 建议提交。

## 烟测证据

- `GET http://10.10.0.17` 返回 `200`，Web 管理端首页可访问。
- `GET http://10.10.0.17:8081/api/health` 返回 `code=200`、`系统运行正常`。
- `GET http://10.10.0.17:8081/api/test/db` 返回 `Kingbase connected successfully`。
- `POST /api/auth/login` 使用说明文档中的学生账号 `20240001 / 123456 / student` 登录成功并返回 token。
- `GET /api/student/info?account=20240001` 返回学生 `张三`、专业 `计算机科学与技术`、`totalCredits=0.0`。
- `GET /api/student/notice/list?studentNo=20240001`、`GET /api/student/party/progress?studentNo=20240001`、`GET /api/student/certificate/history?studentNo=20240001` 均可返回业务数据。
- `POST /api/student/ai/ask` 页面级 Network 复核显示中文问题正常返回；此前命令行烟测中的问号现象按本地显示/编码问题处理，不作为有效 bug。

## Bug 草稿

- `BUG-G16-001`：中文请求字段问号问题已否决，不提交。
- `BUG-G16-002`：请假申请结束日期早于开始日期仍可提交成功并生成待审核记录，建议提交。
- `BUG-G16-003`：提示词注入类异常输入后，后续普通问题仍返回“知识库服务连接异常。”，当前列为候选崩溃类/服务中断问题，需补 Network 状态码和恢复性证据。
- `BUG-G16-004`：党团流程接口返回重复“未申请”阶段，当前列为候选 Logic bug，需页面截图确认是否展示重复阶段。

## 未完成项

- [ ] 在平台中正式登记第 16 组为第 12 组第一阶段测试对象。
- [ ] 使用微信开发者工具导入 `UI` 目录做页面级复现。
- [ ] 为 `BUG-G16-002` 补充页面截图、控制台日志或录屏。
- [ ] 将确认后的 bug 报告录入互评平台。
- [x] 用户已确认 `BUG-G16-002` 已提交到互评平台。
- [x] 新增崩溃类 bug 专项排查清单：`docs/testing/group16-crash-bug-hunting-checklist.md`。
- [ ] 为 `BUG-G16-003` 补充 `/api/student/ai/ask` 的 Network 状态码、响应体、耗时，以及刷新/重新进入后的恢复性测试结论。
- [x] 完成后续安全接口覆盖，产出 `docs/testing/group16-followup-api-coverage-2026-05-28.md`。
- [ ] 为 `BUG-G16-004` 补充“党团事务流程追踪”页面截图，确认重复“未申请”是否在页面可见。

## 边界说明

- 本阶段完成的是说明文档解析和接口级烟测，不等同于完成整组互测。
- 用户提供的第 12 组平台密码未写入仓库、命令参数或测试报告。
- 接口级复现使用的是第 16 组说明文档公开提供的测试账号，不涉及第 12 组平台密码。
- 若接口级烟测与微信开发者工具 Network 证据冲突，以微信开发者工具页面级证据为准。
- 当前第 16 组仍建议继续做崩溃类专项测试；若未发现崩溃类问题，可将已执行清单作为覆盖证据，不应强行构造或刷分。
- `BUG-G16-003` 是否按崩溃类提交，必须以页面不可恢复、请求长时间 pending、接口 500 或刷新后普通问题仍不可用为证据；若仅快速返回错误提示且可恢复，则降级为 Logic bug。
- `BUG-G16-004` 是否提交以页面展示为准；若页面已过滤重复阶段，仅保留为低优先级接口数据问题。
