# Web 前端需求预览入口

- 创建日期：2026-05-17
- 状态：`[x]`
- 关联主计划：`S22.5, S22.6`
- 关联审计：`docs/notes/refinements/2026-05-17-web-requirements-summary-audit.md`

## 目标

提供一个不依赖后端账号或数据库的前端公开预览页，直接验证班团骨干可见菜单范围，以及请假类提示文案。

## 实施清单

- [x] 新增公开路由 `/preview/requirements`
- [x] 在登录页增加开发预览入口
- [x] 新建预览页展示角色切换、可见菜单、受限菜单与请假警示
- [x] 抽取预览用静态数据与判断函数
- [x] 将 `vite` 默认开发端口从 `5173` 调整为 `4173`，规避当前 Windows 环境端口排除区间冲突
- [x] 调整登录页排版，将“学生端请使用小程序登录访问”与“开发预览”拆分为独立辅助区，避免预览按钮被遮挡
- [x] 通过 `web vue-tsc` 与 `vite build`

## 验证

- `web` 类型检查通过
- `web` 构建通过
