# S0 基线冻结细化

- 日期：`2026-04-18`
- 关联主计划：`S0.1, S0.2, S0.3, S0.4`
- 当前状态：`COMPLETED`

## 范围

- 将当前工作区中已存在的有效改动冻结为可执行的原子边界，作为 `S1 ~ S5` 的稳定输入。
- 回跑后端集成测试，以及 `web`、`miniapp` 的构建流程，判断当前基线是否可继续推进。
- 形成一版 `FR/NFR × backend/web/miniapp/tests/docs` 缺口矩阵，用于后续排期、分派与验收追踪。
- 明确并行执行口径、责任分配、推荐分支名与验证证据要求。

## 非范围

- 不在本次细化内修复业务功能缺陷、调整接口契约或新增需求实现。
- 不在本次细化内修改 `docs/notes/current-implementation-plan.md`、SRS 文档或其他说明文档。
- 不在本次细化内执行提交、合并、推送、发版或正式交付排版。
- 不在本次细化内回退他人已存在改动；如发现冲突，只记录边界和阻塞，不直接重写他人工作。

## 执行约束

- 负责人先以角色占位，实际执行时替换为具体姓名或账号。
- Python 环境与依赖统一通过 `uv` 管理；前端依赖与构建按仓库现有锁文件和脚本执行，不引入新的工具链约定。
- `S0.2` 与 `S0.3` 允许并行，但必须建立在 `S0.1.c` 完成后的冻结边界之上。
- 每项验证必须留下可复核证据；若失败，必须同步记录失败范围、阻塞原因和下一步所需输入。

## 任务清单

### `S0.1` 拆分并整理当前工作区中已存在的有效改动，形成原子提交边界

- 推荐分支名：`codex/s0-1-freeze-workspace`
- 负责人：`基线冻结负责人（待指派）`
- 具体文件范围：
  - 当前工作区全部已修改文件与未跟踪文件。
  - 归类口径固定为 `backend / web / miniapp / tests / docs / infra` 六类；若同一文件跨类生效，必须标记为“共享前置”并指定唯一主责。
- 子任务编号：
  - [x] `S0.1.a` 导出工作区改动快照，形成冻结起点说明。
  - [x] `S0.1.b` 按责任域对现有改动进行分桶。
  - [x] `S0.1.c` 判定原子提交边界、共享前置与不可拆分项。
  - [x] `S0.1.d` 为每个边界指定负责人、推荐分支名和后续入口阶段。
- 执行结果：已在根工作区 `codex/s0-freeze-root` 完成冻结，形成 `5088afe`（`FR-005`）与 `f418335`（`FR-018`）两个原子提交，且未混入计划文档改动；剩余工作区改动保留为计划/文档类输入，不影响代码基线冻结结论。
- 测试 / 验证项：
  - 以 `git status --short`、`git diff --name-only`、`git diff --stat` 三份输出交叉复核冻结快照。
  - 复核每个已改文件均有唯一主责；共享文件必须有协同说明，不能出现“归属不明”。
- 依赖顺序：`S0.1.a -> S0.1.b -> S0.1.c -> S0.1.d`
- 风险 / 阻塞：
  - 当前工作区可能存在跨阶段混合改动，导致边界难以一次性拆净。
  - 生成物、临时文件与人工修改混杂时，可能误判有效改动范围。
- 验收条件：
  - 所有已改文件均完成分桶、负责人分配、推荐分支指派与后续阶段挂接。
  - 不存在“待判断是否保留”的文件或未定义责任边界。

### `S0.2` 回跑后端集成测试，确认当前基线可继续推进

- 推荐分支名：`codex/s0-2-backend-baseline`
- 负责人：`后端负责人（待指派）`
- 具体文件范围：
  - 后端应用目录与后端集成测试目录。
  - Python 依赖与运行配置文件，如 `pyproject.toml`、`uv.lock`、环境样例文件及后端测试所需配置。
  - 如存在数据库迁移、测试夹具或测试专用配置，纳入同一验证范围。
- 子任务编号：
  - [x] `S0.2.a` 使用 `uv` 同步后端测试环境，确认依赖与锁文件一致。
  - [x] `S0.2.b` 按仓库现有后端集成测试入口回跑基线测试。
  - [x] `S0.2.c` 记录失败用例、波及模块、最小复现条件与是否稳定复现。
  - [x] `S0.2.d` 输出“可继续推进 / 阻塞”结论，并给出后续入口阶段建议。
- 执行结果：`D:\Codes\super-ruc-wt\s0-backend-baseline\backend` 已执行 `uv run pytest tests/integration -v`，结果 `41 passed in 90.91s`，后端基线可继续推进。
- 测试 / 验证项：
  - 验证 `uv` 环境同步成功，且未产生未预期的依赖漂移。
  - 记录后端集成测试命令、退出码、通过数量、失败数量和关键日志摘要。
  - 如测试依赖数据库、迁移或夹具，需确认前置条件与当前基线一致。
- 依赖顺序：依赖 `S0.1.c` 完成；执行顺序为 `S0.2.a -> S0.2.b -> S0.2.c -> S0.2.d`
- 风险 / 阻塞：
  - 环境变量、数据库快照或夹具缺失，可能导致结果不可复现。
  - 现有失败可能来自历史遗留问题，需与本轮工作区改动区分归因。
- 验收条件：
  - 后端集成测试状态明确为“通过”或“阻塞”，且有证据可复核。
  - 若阻塞，必须给出受影响模块、责任角色、恢复前置和建议进入的后续阶段。

### `S0.3` 回跑 `web` 与 `miniapp` 构建，确认当前前端基线可继续推进

- 推荐分支名：
  - `web`：`codex/s0-3-web-baseline`
  - `miniapp`：`codex/s0-3-miniapp-baseline`
- 负责人：
  - `web 负责人（待指派）`
  - `miniapp 负责人（待指派）`
- 具体文件范围：
  - `web` 相关源码、构建配置、依赖锁文件与脚本定义。
  - `miniapp` 相关源码、构建配置、依赖锁文件与脚本定义。
  - 如存在共享前端契约、共享类型或共享请求封装，纳入两个前端验证的共享前置范围。
- 子任务编号：
  - [x] `S0.3.a` 确认 `web` 构建入口、锁文件与依赖状态。
  - [x] `S0.3.b` 按仓库现有脚本回跑 `web` 构建并记录告警与错误。
  - [x] `S0.3.c` 确认 `miniapp` 构建入口、锁文件与依赖状态。
  - [x] `S0.3.d` 按仓库现有脚本回跑 `miniapp` 构建并记录告警与错误。
  - [x] `S0.3.e` 汇总前端基线结论，识别跨端共享阻塞并挂接后续阶段。
- 执行结果：`miniapp` 在 `D:\Codes\super-ruc-wt\s0-miniapp-baseline` 执行 `pnpm -C miniapp build:mp-weixin` 已通过；`web` 已修正 `src/utils/request.ts` 的 Axios 响应拦截器返回类型，并在 `D:\Codes\super-ruc-wt\s0-web-baseline` 执行 `pnpm -C web build` 通过。
- 测试 / 验证项：
  - 基于现有锁文件完成依赖校验，不新增或替换包管理器约定。
  - 分别记录 `web`、`miniapp` 构建命令、退出码、告警摘要、错误摘要与产物状态。
  - 对共享契约或共享类型引发的问题，明确标记为共享阻塞，避免重复归因。
- 依赖顺序：
  - 依赖 `S0.1.c` 完成。
  - 并行关系为 `S0.3.a -> S0.3.b` 与 `S0.3.c -> S0.3.d` 可并行执行。
  - `S0.3.e` 依赖 `S0.3.b` 与 `S0.3.d` 同时完成。
- 风险 / 阻塞：
  - 前端依赖缓存、锁文件漂移或本地构建环境差异可能导致结果失真。
  - `web` 与 `miniapp` 可能共享同一契约缺陷，若分别处理容易出现重复修复或重复归因。
- 验收条件：
  - `web` 与 `miniapp` 均得到明确的“可继续推进 / 阻塞”结论，并附验证证据。
  - 所有共享阻塞均已单独标识，不在两个端内重复挂账。

### `S0.4` 生成一版需求缺口矩阵：`FR/NFR × backend/web/miniapp/tests/docs`

- 推荐分支名：`codex/s0-4-gap-matrix`
- 负责人：`需求追踪 / QA 负责人（待指派）`
- 具体文件范围：
  - `docs/**` 中现有需求、追踪、验收与说明类文档。
  - `S0.1 ~ S0.3` 产出的冻结清单、测试结论、构建结论与阻塞记录。
  - 与 `FR-001 ~ FR-018`、`NFR-001 ~ NFR-005` 对应的实现、测试、文档映射记录。
- 子任务编号：
  - [x] `S0.4.a` 建立 `FR/NFR × backend/web/miniapp/tests/docs` 矩阵骨架。
  - [x] `S0.4.b` 将 `S0.1 ~ S0.3` 的结果逐项映射到矩阵。
  - [x] `S0.4.c` 为每个交叉项标记 `已覆盖 / 缺口 / 阻塞 / 待确认` 状态。
  - [x] `S0.4.d` 输出优先级、责任角色、建议入口阶段与补证据动作。
- 执行结果：已新增 `docs/notes/s0-gap-matrix-2026-04-18.md`，覆盖 `FR-001 ~ FR-018`、`NFR-001 ~ NFR-005` 的五维矩阵，并给出后续阶段入口建议。
- 测试 / 验证项：
  - 矩阵必须完整覆盖 `FR-001 ~ FR-018` 与 `NFR-001 ~ NFR-005`，不得漏项。
  - 每个矩阵结论均需带有来源证据，证据可来自代码、测试、构建结果或现有文档。
  - 抽样复核至少覆盖一个 `FR` 和一个 `NFR` 的全链路映射，确认矩阵口径可复用。
- 依赖顺序：依赖 `S0.1.d`、`S0.2.d`、`S0.3.e` 全部完成；执行顺序为 `S0.4.a -> S0.4.b -> S0.4.c -> S0.4.d`
- 风险 / 阻塞：
  - 若上游需求条目与当前实现映射关系不够稳定，矩阵状态可能需要二次校正。
  - 若 `S0.2` 或 `S0.3` 仅得到部分结论，矩阵会出现较多“待确认”状态，影响后续排期精度。
- 验收条件：
  - 全部 `FR/NFR` 条目均已映射到矩阵，且每个缺口都有责任角色和建议入口阶段。
  - 矩阵可以直接作为 `S1 ~ S5` 的排期和验收输入，不需要二次重构结构。

## 依赖顺序

1. 先完成 `S0.1.a -> S0.1.b -> S0.1.c -> S0.1.d`，冻结当前工作区边界。
2. 在 `S0.1.c` 完成后，并行推进 `S0.2` 与 `S0.3`。
3. 待 `S0.1.d`、`S0.2.d`、`S0.3.e` 全部完成后，再执行 `S0.4`。
4. 当 `S0.1 ~ S0.4` 全部满足各自验收条件后，`S0` 方可视为完成并作为后续阶段输入。

## 验收条件

- 当前工作区所有有效改动均已完成边界冻结、责任归属和建议分支指派。
- 后端集成测试、`web` 构建、`miniapp` 构建均已有明确结论，并保留可复核证据。
- `FR-001 ~ FR-018`、`NFR-001 ~ NFR-005` 已全部进入缺口矩阵，且每个缺口都能指向责任角色与后续阶段。
- 整个 `S0` 过程中未回退他人改动，所有阻塞均通过记录和分派解决，而不是通过覆盖式修改规避。

## 风险 / 阻塞

- 当前工作区可能已包含跨阶段、跨端、跨文档的混合改动，冻结成本高于单模块基线确认。
- 若环境、锁文件、数据库或构建缓存与仓库基线不一致，测试和构建结论可能存在噪声。
- 若上游需求条目与实现痕迹之间缺少稳定映射，缺口矩阵容易出现“待确认”堆积。
- 若未及时把共享阻塞单独提取，后续阶段会重复接单，增加返工概率。

## 变更记录

- `2026-04-18`：创建 `S0` 基线冻结细化文件，补齐可执行任务树、责任占位、依赖顺序、验证口径与验收条件。
- `2026-04-18`：完成 `S0.1`、`S0.2`、`S0.4` 执行回写；随后修正 `src/utils/request.ts` 的拦截器返回类型，关闭 `S0.3` 的 `web` 构建阻塞。
