# S18 Web 危险主按钮对比度修复

- 日期：`2026-05-16`
- 关联主计划：`S18.1, S18.2, S18.3, S18.4`
- 当前状态：`DONE`

## 范围

- 修复 Web 管理端中 `primary + danger` 按钮出现红色背景叠红色文字的问题。
- 覆盖当前已知受影响按钮：个人信息页 `退出登录`、审计日志页 `执行归档`。
- 保持 Web 现有红色品牌主按钮体系不变，仅修正危险主按钮文字对比度。

## 非范围

- 不修改后端接口、权限逻辑、审计归档业务逻辑或登录退出流程。
- 不调整普通危险链接按钮、普通危险默认按钮和非 Web 端样式。
- 不引入新的按钮组件封装。

## 任务清单

- [x] `S18.1` 使用浏览器检查 `http://127.0.0.1:5174/` 当前渲染，定位红底红字来源。
- [x] `S18.2` 检索 Web 端 `primary + danger` 按钮组合，确认静态命中点。
- [x] `S18.3` 在 `web/src/styles/theme.scss` 收口全局按钮覆盖规则，使普通危险按钮保留红字，危险主按钮固定白字。
- [x] `S18.4` 通过浏览器计算样式与 Web 构建验证修复。

## 验收条件

- 个人信息页 `退出登录` 按钮为红底白字。
- 审计日志页 `执行归档` 按钮为红底白字。
- 普通 `danger` 按钮仍保持红色危险语义。
- `pnpm -C web build` 通过。

## 风险 / 阻塞

- 无阻塞。
- 本次只调整 Web 全局主题覆盖规则；如果后续引入自定义非 Ant Design 按钮，仍需单独检查其对比度。

## 变更记录

- `2026-05-16`：创建文件；完成 `primary + danger` 按钮对比度修复，并通过浏览器与构建验证。
