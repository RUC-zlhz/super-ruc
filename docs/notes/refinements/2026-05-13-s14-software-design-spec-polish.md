# S14 软件设计规格说明书 0513 版完善

- 创建日期：`2026-05-13`
- 关联主计划：`S14.1, S14.2, S14.3, S14.4, S14.5, S14.6`
- 状态：`[x]`
- 输入依据：用户确认的 `软件设计规格说明书 0513 版完善计划`、根目录 `软件设计规格说明书0513.docx`、`docs/templates/软件需求规格说明书模板.docx`、SRS v1.7、S13 主计划证据与当前仓库代码

## 目标

在不覆盖用户已调整的 `软件设计规格说明书0513.docx` 的前提下，生成新的软件设计规格说明书完善版，使正文、图表、目录、版式和验证记录与 S13 后的后端、Web 管理端、学生端小程序、SRS v1.7 与主计划状态保持一致。

## 执行拆分

- [x] `S14.1` 计划登记：在主计划登记 S14，并将本细化文件登记为权威执行计划。
- [x] `S14.2` 内容校准：修正 0513 版中过期的“后端/管理端仅接口契约”口径，补充 Web、Backend、统一进度中心与 S13 官方来源优先实现状态。
- [x] `S14.3` Mermaid 图源完善：更新 `docs/source/diagrams/mermaid/software-design-spec/*.mmd`，覆盖体系结构、界面流、申请顺序、类关系、数据关系和部署设计 6 类图。
- [x] `S14.4` 图像资产生成：生成 Word 兼容 SVG 与 PNG 兜底资产，确保 SVG 不含 `foreignObject`，文字使用真实 `text` 节点。
- [x] `S14.5` Word/PDF 出件：以 0513 版为基线生成 `output/doc/软件设计规格说明书-信息学院学生综合服务与党团管理平台-v1.1.docx`，补自动目录、图题/表题、6 张图和统一排版，并同步导出 PDF。
- [x] `S14.6` 验证与回写：检查 DOCX 结构、图源、SVG、PDF 导出、页面可读性与文档口径，并回写本细化和主计划证据。

## 输出物

- DOCX：`output/doc/软件设计规格说明书-信息学院学生综合服务与党团管理平台-v1.1.docx`
- PDF：`output/doc/软件设计规格说明书-信息学院学生综合服务与党团管理平台-v1.1.pdf`
- 生成脚本：`scripts/docs/build_sds_v11_from_0513.py`
- Mermaid 图源：`docs/source/diagrams/mermaid/software-design-spec/*.mmd`
- Word 兼容图资产：`docs/source/diagrams/rendered/software-design-spec-v1_1/*.svg` 与 `*.png`
- 页面渲染抽检图：`output/doc/qc/sds-v1.1-word-render/page-01.png`、`page-03.png`、`page-10.png`、`page-20.png`、`page-26.png`、`page-32.png`、`page-34.png`

## 验证结果

- DOCX 结构检查：`172` 个正文段落、`25` 张表、`6` 张图，存在可更新 TOC 域；图题连续为 `图 3-1` 至 `图 3-6`。
- 图资产检查：DOCX 内嵌 `6` 个 SVG 和 `6` 个 PNG；正式渲染目录中 `6` 个 SVG 均存在真实 `<text>` 节点，且均不含 `foreignObject`。
- 内容口径检查：DOCX 命中 `V1.1`、`2026 年 5 月 13 日`、`统一进度`、`S13`、`后端 FastAPI`、`PC 管理端`；未命中过期的“接口契约抽象 / 当前代码包主要为学生端小程序 / 代码中尚未包含”等旧口径。
- PDF 检查：Word COM 更新目录并导出 PDF，结果 `34` 页；PDF 文本命中封面、目录、系统总体架构图、核心类关系图、数据实体关系图、系统部署结构图、结语和 `SDS – 1.1`。
- 渲染抽检：通过 Word 页面预览接口导出封面、目录、体系结构、类图、数据图、部署图与结语页 PNG，文件大小均非空，未发现空白页或导出失败。

## 验证要求

- DOCX：存在可更新 TOC 域；标题层级完整；包含 6 张 SDS 图；图题连续为 `图 3-1` 至 `图 3-6`；表格和正文不保留旧口径。
- Mermaid/SVG：6 个 `.mmd` 正式图源存在；6 个 SVG 存在真实 `<text>` 节点且不含 `foreignObject`。
- PDF：导出 PDF 后进行页数与关键页文本/图像抽检，覆盖封面、目录、体系结构图、类/数据图、部署图和结语。
- 计划：主计划与本细化文件均记录输出路径、生成脚本、验证命令和结果。

## 风险与约束

- 不修改原始 `软件设计规格说明书0513.docx`。
- 本阶段只更新文档、图源、渲染资产、生成脚本和计划记录，不修改业务代码。
- 若本机缺少 Mermaid CLI，则使用可复现脚本生成 Word 兼容 SVG/PNG 资产；正式 `.mmd` 仍保留 Mermaid 源语法。

## 变更记录

- `2026-05-13`：创建本细化文件，登记 0513 版 SDS 完善范围、输出物和验证要求。
- `2026-05-13`：完成 S14；已生成 SDS v1.1 DOCX/PDF、6 份 Mermaid 图源、6 份 SVG/PNG 图资产和 7 页关键渲染抽检 PNG，并通过 DOCX/PDF/图源/内容口径检查。
