# S6 Miniapp JPG 视觉对齐优化（Round 2）

- 日期：`2026-04-27`
- 关联主计划：`S6.3`、`S6.4`、`S6.7`
- 当前状态：`COMPLETED`

## 范围

- 继续以 `design/miniapp/` 下 13 张 JPG 为视觉基准，修正上一轮视觉对齐后仍然偏弱的页面骨架、层级、间距和信息密度。
- 优先收紧首页、知识查询、通知中心、通知详情、学业查看、荣誉榜、我的画像、事务申请、发起申请、申请详情、党团进度列表、党团进度详情、理论自测。
- 在不改动既有业务契约的前提下，统一页面顶部主视觉、卡片层级、状态胶囊、底部操作区和详情抽屉观感。

## 非范围

- 不调整后端接口、权限逻辑、数据字段或业务流转。
- 不替换当前设计稿基准，不新增与 JPG 风格无关的视觉方向。
- 不把本轮工作扩展为 Web 管理端重设计。

## 任务清单

- [x] 重新梳理 miniapp 全局视觉骨架，收紧页面背景、主视觉、卡片阴影与状态色阶。
- [x] 针对首页、知识查询、通知中心与通知详情做二次视觉收敛，补足设计稿里的红色主视觉和层级节奏。
- [x] 针对事务申请、申请详情、党团进度与理论自测页面做二次视觉收敛，补足设计稿里的封面卡、时间轴、底部 CTA 与抽屉式详情。
- [x] 针对学业、荣誉、画像页面做二次视觉收敛，保证列表密度、标签、徽章和说明块更贴近 JPG。
- [x] 完成 `vue-tsc` 与 `pnpm -C miniapp build:mp-weixin` 验证，并回写主计划。

## 验收条件

- 主要学生端页面与设计稿在整体色彩、卡片层级、顶部视觉和底部操作区上明显贴近。
- 页面仍可通过 `vue-tsc`，且 `pnpm -C miniapp build:mp-weixin` 成功产出 `dist/build/mp-weixin`。
- 主计划与本细化文件都写回完成状态或阻塞状态。

## 风险 / 阻塞

- 设计稿仅作为视觉基准，未提供可直接复用的图片切图时，需要用 CSS 结构化复现其层级与气质。
- 页面较多，本轮通过 `vue-tsc` 与 `mp-weixin` 出包验证静态正确性；最终视觉仍建议在微信开发者工具中导入 `miniapp/dist/build/mp-weixin` 做真机尺寸复核。

## 验证记录

- `2026-04-28`：执行 `& '.\web\node_modules\.bin\vue-tsc.CMD' --noEmit -p miniapp\tsconfig.json` 通过。
- `2026-04-28`：执行 `pnpm -C miniapp build:mp-weixin` 通过，输出 `miniapp/dist/build/mp-weixin`。

## 变更记录

- `2026-04-27`：创建 Round 2 细化文件，记录用户复核后继续对齐 JPG 视觉基准的增量修正。
- `2026-04-28`：完成 Round 2 视觉收口，补齐知识查询、学业查看、通知详情等上一轮薄弱页面，并通过类型检查与微信小程序出包。
