# S58 小程序党团流程当前节点状态展示修正

- 状态：`[x]` 已完成
- 日期：`2026-05-26`
- 目标：修正学生端党团流程详情页将“已触发的当前节点”显示为“待开始”的误导性文案。

## 生产核查

- 生产学生 `张念昊 / 2024201540` 存在 1 个入党流程实例。
- 流程实例：`workflow_id=1`，模板 `PARTY_DEVELOPMENT_OFFICIAL_V2`，名称“发展党员工作程序（官方29步）”，状态 `ACTIVE`。
- 当前节点：`教育引导`，`current_node_id=16`，节点状态 `PENDING`，但已有 `triggered_at=2026-05-26 12:27:23.345075+00:00`。
- 因此该流程并非未开始；实际含义是“当前节点已触发，尚未完成”。

## 修正内容

- 小程序流程详情页优先使用后端 `current_node_id` 定位当前节点，而不是单纯取第一个非 `DONE` 节点。
- 对节点状态展示增加区分：
  - `PENDING` 且为当前节点或已有 `triggered_at`：显示“进行中”；
  - `PENDING` 且未触发：显示“待开始”；
  - 其他状态继续沿用原有映射。
- 当前节点圆点与状态标签增加进行中样式，避免与后续未触发节点混淆。

## 验证

- `.\web\node_modules\.bin\vue-tsc.CMD --noEmit -p miniapp\tsconfig.json` 通过。
- `pnpm -C miniapp build:mp-weixin` 通过。
