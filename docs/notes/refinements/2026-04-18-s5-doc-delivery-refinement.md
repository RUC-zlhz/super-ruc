# S5 文档与交付闭环细化

- 日期：`2026-04-18`
- 关联主计划：`S5A.1, S5A.2, S5A.3, S5A.4, S5B.1, S5B.2, S5B.3, S5B.4`
- 当前状态：`ACTIVE`

## 范围

- 将主计划中的 `S5A / S5B` 拆解为可执行叶子任务，直接给出分支、负责人、文件范围、验证项、依赖顺序、风险与验收口径。
- 覆盖 `S5A 追踪矩阵与上游文档闭合`、`S5B SRS v1.6 正式交付件` 两个阶段。
- 明确可并行与必须串行的执行关系，便于后续按分支或 `git worktree` 拆分执行。
- 保持“新版本建立在旧版本基础上”的原则，不预设改动已调整好的内容和排版。

## 非范围

- 不在本文件中直接执行任何文档修改、图修改、导出或排版操作。
- 不在本文件中承接 `S0 ~ S4` 的功能实现与代码修复，只记录它们对 `S5` 的前置依赖。
- 不在本次会话中回写 `docs/notes/current-implementation-plan.md` 的细化文件登记表。
- 不臆造具体成员姓名；负责人统一以“角色负责人”形式指派，待执行时映射到实际人员。

## 执行顺序

1. `S5A-01` 与 `S5A-02` 可并行起草。
2. `S5A-03` 依赖 `S5A-01 + S5A-02` 收口。
3. `S5A-04` 依赖 `S5A-03`，且依赖 `S0 ~ S4` 中对应验收缺口已闭合。
4. `S5B-01` 与 `S5B-02` 可并行准备，但最终签收必须晚于 `S5A-04`。
5. `S5B-03` 依赖 `S5B-01 + S5B-02`。
6. `S5B-04` 依赖 `S5B-03`。

## 任务清单

### S5A 追踪矩阵与上游文档闭合

#### [ ] S5A-01 补 `CP-011 / CP-012`

- 推荐分支名：`codex/s5a-cp-011-012`
- 负责人：`需求文档负责人`
- 具体文件范围：
  - `01-customer-problems.md`
- 测试 / 验证项：
  - `CP-011`、`CP-012` 均补齐为完整条目，而非只补标题。
  - 新增条目的编号、措辞风格、粒度与既有 `CP` 条目保持一致。
  - 新增条目可被后续 `CN-014 / CN-015` 直接追踪。
- 依赖顺序：
  - 顺序号 `1`
  - 可与 `S5A-02` 并行起草，但合并前需统一口径。
- 风险 / 阻塞：
  - 若 `CP` 描述与既有 `FR-017 / FR-018` 口径不一致，会导致后续追踪矩阵再次断链。
  - 若只补标题、不补完整结构，`S5A-03` 无法真实收口为全绿。
- 验收条件：
  - `CP-011 / CP-012` 已补齐且编号唯一。
  - 描述可被 `CN-014 / CN-015` 直接承接，无歧义或重复定义。

#### [ ] S5A-02 补 `CN-014 / CN-015`

- 推荐分支名：`codex/s5a-cn-014-015`
- 负责人：`需求文档负责人`
- 具体文件范围：
  - `03-customer-needs.md`
- 测试 / 验证项：
  - `CN-014`、`CN-015` 均补齐为完整条目，而非仅在索引或矩阵中挂号。
  - 新增 `CN` 严格承接新增 `CP`，不额外引入未确认的新需求。
  - 新增 `CN` 可分别映射到 `FR-017 / FR-018`。
- 依赖顺序：
  - 顺序号 `1`
  - 可与 `S5A-01` 并行起草；最终定稿前需回看 `S5A-01` 的措辞。
- 风险 / 阻塞：
  - 若 `CN` 粒度过大，会破坏 `CP -> CN -> FR` 的一一承接关系。
  - 若 `CN` 口径超出当前 SRS 已确认范围，会把 `S5A` 扩大成新增需求定义。
- 验收条件：
  - `CN-014 / CN-015` 已补齐且可直接追踪到新增 `CP`。
  - 新增 `CN` 与现有 `FR` 映射关系清晰，不形成悬空需求。

#### [ ] S5A-03 收口 `traceability-matrix.md`

- 推荐分支名：`codex/s5a-traceability-green`
- 负责人：`追踪矩阵负责人`
- 具体文件范围：
  - `traceability-matrix.md`
  - 只读核对：`01-customer-problems.md`、`03-customer-needs.md`
- 测试 / 验证项：
  - `Completeness Check` 中与 `CP-011 / CP-012`、`CN-014 / CN-015` 相关的非绿色项全部清零。
  - `Gap Analysis` 中与“上游文档未补齐”相关的项全部收口。
  - `CP -> CN -> FR -> NFR` 映射链在矩阵中保持自洽。
- 依赖顺序：
  - 顺序号 `2`
  - 依赖 `S5A-01 + S5A-02`。
- 风险 / 阻塞：
  - 若上游文档补齐后，矩阵措辞未同步，仍会保留“待上游”或“未写入”类残留。
  - 若 `S0 ~ S4` 侧仍存在会影响追踪链结论的未决项，则“全绿”只能停留在文字层，不能形成真实闭环。
- 验收条件：
  - `Completeness / Gap Analysis` 对上游文档缺口的描述全部收口。
  - 矩阵不再出现与 `CP-011 / CP-012 / CN-014 / CN-015` 相关的断链说明。

#### [ ] S5A-04 重跑 `v15-acceptance-walkthrough.md`

- 推荐分支名：`codex/s5a-acceptance-closeout`
- 负责人：`验收负责人`
- 具体文件范围：
  - `v15-acceptance-walkthrough.md`
- 测试 / 验证项：
  - 对原有 `❌ / ⚠️` 项逐条复核，不允许仅批量替换图标。
  - 每个由 `❌ / ⚠️` 转 `✅` 的结论都必须能对应到实现或验证证据。
  - 文档中的最终结论需与 `S5A-03` 的追踪矩阵一致。
- 依赖顺序：
  - 顺序号 `3`
  - 依赖 `S5A-03`。
  - 额外依赖：主计划中与当前验收缺口对应的 `S0 ~ S4` 条目已完成，否则不得强行改成 `✅`。
- 风险 / 阻塞：
  - 若实现侧仍有缺口，则本任务会被真实阻塞，不能以“先改文档后补实现”的方式伪闭环。
  - 若测试证据不足，则状态只能维持 `⚠️` 或 `[!]`，不能强行全绿。
- 验收条件：
  - 原有 `❌ / ⚠️` 全部有证据地收口为 `✅`。
  - 文档结论可回溯到实现、测试或构建结果，而非口头声明。

### S5B SRS v1.6 正式交付件

#### [ ] S5B-01 按模板重排版 `SRS v1.6`

- 推荐分支名：`codex/s5b-srs-layout`
- 负责人：`交付排版负责人`
- 具体文件范围：
  - `SRS v1.6` 主文档源文件
  - `软件需求规格说明书模板.docx`
- 测试 / 验证项：
  - 章节层级、编号、目录、图表标题、页眉页脚、分页规则逐项对照模板。
  - 版式调整只能建立在旧版基础上，不破坏已确认的内容与布局。
  - 重排版后，图号、表号、引用号仍保持一致。
- 依赖顺序：
  - 顺序号 `4`
  - 建议依赖 `S5A-04` 完成后再正式定稿。
- 风险 / 阻塞：
  - 若 `S5A` 尚未冻结，排版会因内容回流反复返工。
  - 若为追求模板一致性而大范围重排，可能破坏用户已调好的版式。
- 验收条件：
  - `SRS v1.6` 符合模板要求。
  - 不存在明显编号错乱、分页失控或交叉引用错位。

#### [ ] S5B-02 Mermaid 图核对与拆图

- 推荐分支名：`codex/s5b-mermaid-audit`
- 负责人：`图示负责人`
- 具体文件范围：
  - 所有 Mermaid 源图
  - `SRS v1.6` 中对应插图位置
- 测试 / 验证项：
  - 每张图必须与最终实现和文档表述一致，不允许保留历史版本图义。
  - 图的信息密度过大时必须拆图，优先保证竖版阅读可用性和箭头排布整齐。
  - 输出图必须遵守“Mermaid 语法 + 使用 `mmd` 生成 `svg` + 文字节点为 `text` 而非 html/thml 标签”的约束。
- 依赖顺序：
  - 顺序号 `4`
  - 可与 `S5B-01` 并行，但最终签收必须晚于 `S5A-04`。
- 风险 / 阻塞：
  - 若图与实现存在漂移，导出件会形成“文档正确、图错误”的交付缺陷。
  - 若 `svg` 输出仍依赖 html 标签，Word 兼容性会直接受损。
- 验收条件：
  - 所有 Mermaid 图与实现一致。
  - 生成的 `svg` 在 Word 中具备可读性与兼容性。

#### [ ] S5B-03 导出 `docx / emf 变体 / pdf`

- 推荐分支名：`codex/s5b-export-bundle`
- 负责人：`导出负责人`
- 具体文件范围：
  - `SRS v1.6` 导出产物
  - `docx`
  - `emf` 变体
  - `pdf`
- 测试 / 验证项：
  - 三类交付件都必须逐一打开检查，而不是只验证文件存在。
  - 导出后需核对字体、图、分页、标题层级、表格边界和交叉引用是否稳定。
  - `emf` 变体与基础 `docx / pdf` 的内容结论必须一致。
- 依赖顺序：
  - 顺序号 `5`
  - 依赖 `S5B-01 + S5B-02`。
- 风险 / 阻塞：
  - 导出链路可能出现丢图、字体替换、分页漂移或 `emf` 兼容性异常。
  - 若图源未先在 `S5B-02` 收口，导出只会放大兼容性问题。
- 验收条件：
  - `docx / emf 变体 / pdf` 均成功导出且可打开。
  - 关键版式与结论在各导出件中保持一致。

#### [ ] S5B-04 最终可读性与一致性检查

- 推荐分支名：`codex/s5b-final-qc`
- 负责人：`交付质检负责人`
- 具体文件范围：
  - `SRS v1.6` 成稿
  - 全部导出件
- 测试 / 验证项：
  - 页级可读性检查：标题、分页、图表、空白密度、跨页表格、图文相邻关系。
  - 一致性检查：版本号、日期、修订历史、章节引用、图表编号、术语前后一致。
  - 结论一致性检查：文档、图、实现、测试结论四者一致。
- 依赖顺序：
  - 顺序号 `6`
  - 依赖 `S5B-03`。
- 风险 / 阻塞：
  - 若最终包内部版本号、日期或图文口径不一致，会直接破坏“正式交付件”属性。
  - 若在该阶段才发现结构性问题，通常需要回退到 `S5B-01` 或 `S5B-02` 重做。
- 验收条件：
  - 交付件整体可直接提交。
  - 不存在明显排版、图文、版本或结论不一致问题。

## 验收条件

- `S5A` 完成后，上游文档、追踪矩阵、验收走查三者口径一致。
- `S5B` 完成后，`SRS v1.6` 排版、Mermaid 图、导出件与最终测试结论一致。
- `S5` 总出口条件满足主计划定义：
  - 文档、图、实现、测试结论四者一致。
  - 可直接作为正式交付件。

## 风险 / 阻塞

- `[!]` 当前会话被限定为“只写本细化文件”，因此主计划中的“细化文件登记表同步回写”无法在本次同时完成，需后续单独补登记。
- `[!]` `S5A-04` 的“全绿”依赖 `S0 ~ S4` 的真实实现与验证闭环；若前序工作未完成，`S5A` 只能停留在部分完成。
- `[!]` `S5B-02 / S5B-03` 依赖 Mermaid 渲染与导出链路稳定；若 `svg` 仍含 html/thml 标签或 `emf` 变体不稳定，将直接阻塞正式交付。
- `[!]` 若执行中出现“为过模板检查而破坏既有版式”的情况，应视为质量回退，而不是完成。

## 变更记录

- `2026-04-18`：创建文件，登记 `S5A / S5B` 可执行任务树、推荐分支、负责人、依赖顺序、风险与验收口径。
