# S22 培养方案明细与 CRUD 界面补齐

- 状态：`[x]` 已完成
- 关联主计划：`S22.1, S22.2, S22.3, S22.4, S22.5`
- 输入依据：用户指出培养方案页面应支持点开细节并进行增删改查操作。

## 问题

`/academic/curriculum` 页面此前只展示培养方案模块表，虽然后端 `CurriculumModule.courses` 已保存课程明细，前端没有提供展开查看，也没有把后端已有的 `POST/PATCH/DELETE /admin/curriculum/plans` 能力暴露为可用的维护操作。

## 执行项

- [x] `S22.1` 复核后端培养方案接口，确认可复用现有方案级 `GET/POST/PATCH/DELETE`，其中 `PATCH` 支持整份方案连同模块与课程数组覆盖保存。
- [x] `S22.2` 将模块表改为可展开表格，展示模块内课程编码、课程名称、学分、开课学期和行级操作。
- [x] `S22.3` 补齐方案新增、方案编辑、方案删除、模块新增、模块编辑、模块删除、课程新增、课程编辑、课程删除入口。
- [x] `S22.4` 复用现有 `PATCH /admin/curriculum/plans/{id}` 保存模块与课程变更，并完成 Web 构建与浏览器页面检查。
- [x] `S22.5` 补齐培养方案切换加载态隔离与 `updated_at` 乐观冲突校验，避免旧详情在加载中被误操作或多标签页静默覆盖。

## 验证

- Web 构建：`pnpm -C web build` 通过。
- 后端回归：`UV_CACHE_DIR=D:\Codes\super-ruc\.uv-cache-local` 下执行 `uv run pytest tests\integration\test_s12_gap_closure.py -q`，结果 `5 passed in 82.99s`。
- 后端加固回归：同一测试已新增培养方案 `expected_updated_at` 冲突断言，覆盖旧详情提交冲突保护。
- 浏览器检查：`http://127.0.0.1:5174/academic/curriculum` 已显示 `新增方案`、`新增模块`、`编辑方案`、`删除方案`，模块表每行显示课程数量并提供 `新增课程 / 编辑 / 删除`，表格包含展开入口。

## 结论

培养方案页已从只读模块列表升级为可维护工作台；课程明细随模块展开查看，方案、模块、课程的增删改操作均通过当前后端权威接口保存，并补上切换加载态隔离与并发冲突保护。
