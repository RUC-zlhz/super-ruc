# S50 第 12 组互测使用说明出件

- 关联主计划：`S50.1, S50.2, S50.3`
- 当前状态：`[x]` 已完成
- 记录日期：`2026-05-25`

## 1. 背景

根据《测试实验指导书》，第二轮互测需要每个小组额外提供可操作的使用说明文档，帮助其他小组快速访问、理解并测试项目；若文档缺失或说明不清导致无法运行、无法访问或无法完成关键路径，也可能被计为崩溃类问题。

本次任务需要基于《基本功能文档》与当前 `super-ruc` 实际可用环境，输出一份面向其他小组的《第12组-super-ruc-互测使用说明》，重点覆盖：

- 当前稳定测试入口
- 可直接使用的共享账号
- 推荐测试路径与示例输入
- 默认数据状态与已知限制
- 小程序和本地环境的补充测试方式

## 2. 输入与核实范围

### 2.1 上游输入

- `D:/xwechat_files/wxid_gzu1gg4rh6p22_a5f4/msg/file/2026-05/测试实验指导书.pdf`
- `C:/Users/曾翎一/Desktop/基本功能文档.docx`
- `docs/templates/软件需求规格说明书模板.docx`

### 2.2 本次核实的运行态事实

- `[x]` `http://10.10.0.13/healthz` 返回 `{"code":0,"message":"ok","data":{"status":"ok"}}`
- `[x]` `http://10.10.0.13/` 返回 `HTTP 200`
- `[x]` `POST http://10.10.0.13/api/v1/auth/login` 使用 `admin / admin123` 可登录
- `[x]` 当前 Web 登录后会提示修改默认密码，但点击“稍后处理”仍可继续进入系统
- `[x]` 当前默认数据状态：
  - 学生主档 `5` 条
  - 党团流程模板 `2` 套
  - 学生流程实例 `0` 条
  - 已发布通知 `1` 条
  - 培养方案 `7` 条
  - 已发布知识条目 `0` 条
- `[x]` 历史临时公网地址 `http://123.57.54.195/` 当前返回 `502`，不再作为有效入口写入说明文档

## 3. 实施拆分

### 3.1 `S50.1` 互测信息整理

- 从指导书提炼互测文档要求：访问方式、示例输入输出、已知限制、平台提交说明。
- 从基本功能文档提炼 `super-ruc` 的 5 个核心功能模块，确定说明文档应优先覆盖 Web 管理端链路，并将小程序作为补充路径。
- 对当前实际部署环境做可达性与账号实测，避免把失效地址写入文档。

### 3.2 `S50.2` 文档生成

- 新增脚本：`scripts/docs/build_peer_testing_usage_guide.py`
- 生成出件：`output/doc/第12组-super-ruc-互测使用说明.docx`
- 版式基于 `docs/templates/软件需求规格说明书模板.docx`，保留模板页边距、层级标题与表格风格。
- 文档内容覆盖：
  - 引言
  - 测试前准备
  - Web 管理端测试说明
  - 小程序与本地环境说明
  - 已知限制与反馈建议

### 3.3 `S50.3` 页面 QC

- 初版生成后，使用本机 Word 导出 PDF，再转为 PNG 页面进行人工检查。
- 根据渲染结果做了两轮收口：
  - 将说明性条目从整块单列表格改为紧凑段落，缓解目录与说明块的跨页拥挤。
  - 对表格行增加“整行不拆页”，避免步骤说明被硬拆。
  - 合并末页过短条目，去除空尾页。

## 4. 交付结果

### 4.1 正式出件

- `output/doc/第12组-super-ruc-互测使用说明.docx`

### 4.2 文档内明确写入的关键测试信息

- 远端优先入口：`http://10.10.0.13/`
- 健康检查：`http://10.10.0.13/healthz`
- API 前缀：`http://10.10.0.13/api/v1`
- 共享管理员账号：`admin / admin123`
- 共享账号提示：若弹出修改初始密码提醒，应点击“稍后处理”，不要改掉共享密码
- 本地一键启动脚本：`.\scripts\dev\start-dev.ps1`
- 小程序本地 mock 登录示例学生：`2024202721 / 曾翎一`

## 5. 验证

- `[x]` `scripts/docs/build_peer_testing_usage_guide.py` 成功生成 DOCX
- `[x]` 使用本机 Word 成功导出 PDF
- `[x]` PDF 成功渲染为 `9` 页 PNG 页面
- `[x]` 已人工复核封面、目录、表格、步骤区和末页，无遮挡、无空尾页、无明显跨页断裂问题

## 6. 备注

- 本次文档按“其他小组快速上手测试”目标编写，因此明确将 Web 管理端设为推荐首入口，小程序测试仅作为补充项。
- 由于当前默认数据中知识条目和学生流程实例均为空，文档已显式提示这两类场景需要测试者先创建测试数据，避免被误判为系统崩溃。
