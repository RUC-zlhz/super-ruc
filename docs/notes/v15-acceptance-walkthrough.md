# v1.5 验收走查清单（2026-04-22 收口版）

**生成时间**: 2026-04-22  
**覆盖范围**: FR-001~018、NFR-001~005、`docs/notes/fix.md` 四条 v1.5 补充点、`ICR-004 Kingbase`  
**来源**: 当前仓库实现、`docs/notes/current-implementation-plan.md` 中 `S1 ~ S5` 已回写证据、当日隔离 Kingbase gate 与 `v1.6` delivery gate 实跑结果  
**说明**:
- `✅` 已有实现与当前证据闭环
- 本版为收口版，当前不保留 `⚠️ / [!]` 项

---

## 一、当前证据快照

- `S1` 已完成：`backend` 集成测试、`web` 构建、`miniapp` 构建均已回写主计划。
- `S2` 已完成：`uv run pytest tests/integration -q` 回写为 `47 passed, 1 warning in 133.12s`；通知、申请/证明、学业分析与运营看板闭环均已落地。
- `S3` 已完成：`uv run pytest tests/integration -q` 回写为 `48 passed in 117.20s`；荣誉与画像闭环已完成二次收口。
- `S4` 已完成：`& '.\backend\scripts\dev\run_s4_kingbase_gate.ps1' all -SkipSync -DbMode pg` 已在隔离 `127.0.0.1:54323` Kingbase 实例上依次通过 `migrate / seed / tests / benchmark`；`S4` 集成回归结果为 `44 passed, 1 warning`，导入 benchmark 中位数为 `median_total_seconds=0.445476`、`median_commit_seconds=0.337598`、`median_validate_seconds=0.107221`。
- `S5` 已完成：`& '.\scripts\srs\v1_6\run_v16_delivery_gate.ps1' -Force` 已在提权环境下全链通过，生成 `v1.6`、`v1.6-emf`、`v1.6-emf-inkscape` 三组 `docx / pdf` 共 `6` 个正式交付件；三份 PDF 均为 `36` 页 A4，两份 EMF 变体 docx 均内嵌 `13` 个 `.emf`。

---

## 二、FR-001 ~ FR-018 状态矩阵

| FR | 标题 | 当前状态 | 复核结论 |
|----|------|----------|----------|
| FR-001 | 政策与流程查询 | ✅ | `knowledge/search`、详情接口与学生端查询页已稳定接通。 |
| FR-002 | 权威答复治理 | ✅ | AI 匹配、权威来源、版本治理与管理端上下架均已落地。 |
| FR-003 | 知识与模板维护 | ✅ | 知识条目、来源、模板上传/停用/下载与条目绑定均已具备；模板入口与知识页集成展示，不影响 FR 闭合。 |
| FR-004 | 党团进度查看 | ✅ | 学生端“我的流程/详情”与当前阶段可见性已闭环。 |
| FR-005 | 党团提醒管理 + 理论自测 | ✅ | 节点提醒、题库维护、三题型答题与判分链路已落地并有测试覆盖。 |
| FR-006 | 事务在线提交 + PDF 预览 | ✅ | 学生端两步式草稿/附件/提交、证明 PDF 预览与线下提示边界均已接通。 |
| FR-007 | 申请审核工作台 | ✅ | 管理端结构化审批页、附件/历史/预览查看与转线下动作均已落地。 |
| FR-008 | 驳回撤回重提 | ✅ | 驳回重提、撤回、转线下与共享文案已统一，三类典型流程测试已覆盖。 |
| FR-009 | 文件导入导出 + 整批回滚 + 错误 Excel | ✅ | 原子提交、错误报告、整批回滚与导入导出治理链路已落地。 |
| FR-010 | 通知标签 + 目标人群 | ✅ | 管理端已支持标签规则、命中预览与目标人群圈选；后端 contract 与学生端已闭环。 |
| FR-011 | 通知发送与接收记录 | ✅ | 发送批次、投递明细、学生收件箱、已读留痕与管理端回看能力已落地。 |
| FR-012 | 角色与字段级权限 | ✅ | 字段级权限矩阵、脱敏、导出 enforcement 与敏感路径回归已在隔离 Kingbase gate 中完成真实 DB 验证。 |
| FR-013 | 审计日志 | ✅ | 审计查询、`entity_id/action` 检索、当前+历史范围、手工/定时归档及审计访问控制均已在隔离 Kingbase gate 中闭环。 |
| FR-014 | 学业缺口（弱结论） | ✅ | 学生端 canonical academic-gap、管理端聚合查询/筛选/明细下钻与弱结论提示均已落地。 |
| FR-015 | 培养方案规则维护 | ✅ | 方案/开课/等价规则维护已在管理端闭环，并作为学业分析基础能力使用。 |
| FR-016 | 运营统计看板 | ✅ | canonical overview、图表、空态与 academic-gap 汇总卡片均已替换占位实现。 |
| FR-017 | 奖励荣誉公示 | ✅ | 类别维护、学年/类别筛选、批量导入、归档/撤销、历史荣誉展示与治理留痕均已闭环。 |
| FR-018 | 学生画像 + 账号生命周期 | ✅ | 画像聚合、越权审计、非在读只读、纠错/补录闭环、快照导出与 scope 校验均已落地。 |

### 小结（FR 层）

- 完整 `✅`：18 项
- 保留 `⚠️`：0 项
- 缺失 `❌`：0 项

---

## 三、NFR-001 ~ NFR-005

| NFR | 当前状态 | 复核结论 |
|-----|----------|----------|
| NFR-001 敏感数据安全 | ✅ | 加密、默认字段策略、脱敏、导出控制与敏感路径回归已在隔离 Kingbase gate 中闭环。 |
| NFR-002 审计留存与可追溯性 | ✅ | 手工归档、运行时定时归档、Redis 锁、防重执行、环境开关与运维文档已补齐。 |
| NFR-003 常见操作响应时间 | ✅ | 索引迁移、跨方言排序 helper 与 `100` 行导入 benchmark 已在隔离 Kingbase gate 中实跑，并记录中位数 `0.445476s / 0.337598s / 0.107221s`。 |
| NFR-004 事务一致性与数据可靠性 | ✅ | 导入回滚、错误报告与关键事务原子性设计已在当前实现中保持。 |
| NFR-005 学生与老师的操作易用性 | ✅ | canonical 响应结构、主要闭环页面与统一文案已收口；当前未发现阻断交付的可用性缺口。 |

---

## 四、`fix.md` v1.5 四条补充点

| # | 条目 | 当前状态 | 复核结论 |
|---|------|----------|----------|
| ① | 账号生命周期（`enrollment_status` 全局拦截） | ✅ | `profile` 学生纠错入口已接入 `ActiveStudentDep`，管理端/学生端均已体现非在读只读与历史状态。 |
| ② | 审计冷备份 | ✅ | 手工归档 API、运行时调度器、归档后查询范围与运维配置已完整落地。 |
| ③ | 涉密转线下（`OFFLINE_HANDLED`） | ✅ | 审批状态机、管理端动作、学生端线下提示卡片与联系方式展示已闭环。 |
| ④ | 行级错误报告 Excel | ✅ | 导入错误原因、下载错误报告与相关审计动作均已落地。 |

---

## 五、`ICR-004` Kingbase 回归

| 项目 | 当前状态 | 复核结论 |
|------|----------|----------|
| `S4C.1` 零库迁移 | ✅ | 隔离 Kingbase 零库已通过 `uv run alembic upgrade head`，主表、索引与历史表创建成功。 |
| `S4C.2` 核心 CRUD / 导入 / 查询回归 | ✅ | `run_s4_kingbase_gate.ps1` 已在隔离实例上通过核心 CRUD、批量导入、关键查询与审计访问控制回归。 |
| `S4C.3` 兼容性记录与风险归档 | ✅ | Kingbase 兼容结果、脚本入口与 benchmark 已回写到主计划及 `S4` 细化文件，不再保留未归属风险。 |

---

## 六、收口结论

- 与 `2026-04-21` 的严格 gated 复核版相比，`FR-012 / FR-013 / NFR-001 / NFR-003 / ICR-004` 已获得真实数据库与性能证据，当前可正式收口为 `✅`。
- `S4` 与 `S5` 的阻塞链路已通过隔离 Kingbase gate 和 `v1.6` delivery gate 两条脚本化入口关闭；当前主线不再依赖历史 `54322` 拒连口径。
- 本文档现作为 `v1.5` 验收走查的收口版保留；后续若继续迭代，只允许在本版结论基础上追加新的变更证据。
