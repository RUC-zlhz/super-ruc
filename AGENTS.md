## user preferences:

1. 如果需要你可以开启任意多的subagent 使用 gpt-5.4 xhigh
2. 图要求
   1. 美观 参照现有图的init参数，尽量箭头排版整齐，尽量采用易于在 word中查看的竖版排版，如果你认为一个图的信息密度过大你可以拆分为多个图
   2. 规范 严格按照软件工程规范实现
   3. 无幻觉 所有内容必须严格按照文档落实。
   4. 使用 mermaid 语法绘图。使用mmd命令生成svg图像，并保证文字等内容应是text而非thml标签，以确保在 word 中的兼容显示。
3. 内容要求：
   1. 新版本的内容必须是在旧版本基础上建立的，不允许修改我已经调整好的内容和排版，除非我明确要求你修改。
4. 排版：
   1. 版式要求严格按照 软件需求规格说明书模板.docx
   2. 在完成所有修改后，按照模板要求进行排版调整，确保最终文档的专业性和一致性，然后自行检查页面的可读性等。

## 持久化计划与细化规则

1. 当前对话确认的全局实现计划，统一保存在 `docs/notes/current-implementation-plan.md`，它是后续实现与汇报的权威主文件。
2. 后续任何新确认的细化计划、局部实现方案、范围调整或阶段拆分，都必须新增为独立文件，放在 `docs/notes/refinements/` 下，并在主计划文件中登记关联关系与状态。
3. 每次完成一段实质性工作后，都必须回写主计划文件和相关细化文件，明确标记状态：
   - `[ ]` 未开始
   - `[-]` 进行中
   - `[x]` 已完成
   - `[!]` 阻塞
4. 新对话开始时，必须先读取 `docs/notes/current-implementation-plan.md`，如有相关细化文件，也必须一并读取，再继续规划或实施。
5. 不允许静默丢弃旧计划或旧细化。若方案被替换，只能标记为“已替代/已失效”，并留下替代文件或替代条目的引用。
